public static void insercao(Item v[], int n) {
  int j;
  for (int i = 2; i <= n; i++) {
    Item x = v[i];
    j = i - 1;
    v[0] = x; /* sentinela */
    while (x.chave() < v[j].chave()) {
      v[j + 1] = v[j];
      j--;
    }
    v[j + 1] = x;
  }
}
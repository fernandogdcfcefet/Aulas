public static void bolha(Item v[], int n) {
  int i, j;
  Item temp;
  for (i = 1; i <= n - 1; i++) {
    for (j = 1; j <= (n - i); j++) {
      if (v[j].chave() > v[j + 1].chave()) {
        temp = v[j];
        v[j] = v[j + 1];
        v[j + 1] = temp;
      }
    }
  }
}
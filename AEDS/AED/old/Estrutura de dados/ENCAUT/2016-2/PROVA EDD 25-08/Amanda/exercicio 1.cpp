#include<stdio.h>
#include<stdlib.h>
#include<conio.h>
#define tam 3
int norma(int mat[tam][tam],int tam)
{
	int i,j;
	int soma=0;
	for(i=0;i<tan;i++)
	{
		for(j=0;j<tan;j++)
		{
			soma=soma[i]+ mat[i][j];
			
		}
		
	}
	for(i=0;i<tan;i++)
	{
		if(soma>soma[0])
		{  int maior;
			soma=maior;
		}
	} 
	
 	printf("%d",soma);
 	
 return soma;	
	
}

int main()
{  
   int mat[tam][tam];
   int tamanho[tam];
   norma(mat,tamanho);
   
	return 0;
}

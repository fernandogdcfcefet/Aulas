#include <stdio.h>
#include <stdlib.h>
#define N 3
#define X 3

void cadastroAlunos(int matricula, char nome, float notas){

    int alunos[N];
    for(int i=0;i<N;i++){
    printf("Digite o numero de matricula do aluno: \n");
    scanf("%d",alunos[i].&matricula);
    printf("Digite o nome do aluno: \n");
    scanf("%c",alunos[i].&nome);
    printf("Digite a nota do aluno: \n");
    scanf("%f",alunos[i].&notas);
                        }
               }

void calcularMedia(float media = 60, float *notas, float soma=0){
    soma =+notas[i];
    for(int=0;i<N;i++){
        if(notas[i]>=60){
            printf("Aprovado| Aluno: %d, nota: %f", i, notas[i]);
        }

}

int main()
{

    printf("INICIALIZANDO O PROGRAMA\n");

    cadastroAlunos(matricula, nome, notas);
    calcularmedia(media, notas, soma);

    return 0;
    }





